<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="ListViewItem" x:Key="SongLibraryTheme">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <!--
                    <Button Height="80"
                            Width="80"
                            Content="{Binding text}"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Foreground="White"
                            Background="{TemplateBinding Background}"
                            Command="{Binding ElementName=ListView1, Path=DataContext.test3}"
                            CommandParameter="{Binding text}"/>
                    -->

                    <RadioButton
                        GroupName="Sliders"
                        Style="{StaticResource SongTheme}"
                        Command="{Binding ElementName=ListViewSongs, Path=DataContext.SelectSongCommand}"
                        Margin="8,0,0,0"
                        CommandParameter="{Binding number}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>